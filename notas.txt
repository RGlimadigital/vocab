Notas de projeto:
-app.use()
Serve para modificar os middlewares que eu importo para o meu projeto, 
utilizando a instancia do express 'app'.


-req.originalUrl
Me develve a url raiz do projeto.

-MongoDb
Sempre conect o servidor de mongo antes de iniciar o projeto, 
utilize o compass para isso.

-Create
1-Usando o postman , com o  verbo POST.
2-Entre com a url: http://localhost:3000/words 
3-Selecione a opcao body e escolha : x-www-form-urlencoded 
4-Entre com o par chave-valor:
A chave seria a key do meu schemma e a palavra o seu valor.
Tambem funciona se diretamente em Raw em escrevo um json com o mesmo par.

-Read
GET
-Lista com todas as palavras: http://localhost:3000/words
-Palavra especifica: http://localhost:3000/words/5ed8a29fa4826e04a1450c11

Criando o Front End em VUEJS
O back end foi criado na pasta SERVER , agora vamos criar o front-end na pasta front-end.
Assim fica mais facil de organizar o projeto.

-Vamos usar o vue CLI 
1-Na pasta raiz do projeto: vue create front-end
2-Confiuramos o projeto de acordo com o indicado no curso 

-Usando o link ativo com vue-router:
linkActiveClass: 'active', que trabalha em conjunto com semantic-ui-css.


-Comunicando com o Back End:
Temos que estabelecer um método de comunicaçao com o Node no Back End.
Essa comunicaçao sera feita com axios, que ja foi instalada.

Vamos manter toda nossa logica de Ajax em um mesmo lugar, e caso queiramos 
mudar de axios para outra livraria , sera muito facil.

Toda a informacao do Axios vai estar no arquivo helper.js.

Recebendo dados desde o servidor e os colocando em uma variavel
 Tenho que revisar a funcao do $route
 this.word = await api.getWord(this.$route.params.id)
 
 
 Vamos Criar uma nova palavra :
 -Modificamos o arquivo New.vue

 Deletando uma palavra:
 -O mais legal foi usar a caixa de dialogo pra poder deletar a palavra , pois nela 
 ja esta implicita uma variavel boolean que podemos manipular.

 Testando as palavras:
  



